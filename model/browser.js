/*
import mongoose from "mongoose"

export const browserSchema = new mongoose.Schema({
    userId: {type : String,ref : 'User'},
    running : Boolean,
});


export const BrowserModel = new mongoose.model("browser", browser);

browser.deleteMany({});
*/